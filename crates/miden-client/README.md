# miden-client

This crate implements a client-based service. It re-exports the core
`miden-client-core` APIs and adds a background loop that can continuously
synchronize with the chain, react to events, and serialize transaction
execution.

TODO: expand
TODO: diagram
